import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  wrapper: {
    flex: 1,
    color: '#FFF',
    backgroundColor: 'rgba(0, 0, 0, 0.5)',
  },

  imageBackground: {
    width: '100%',
    height: '100%',
  }
});
